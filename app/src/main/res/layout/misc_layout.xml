<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <include
        android:id="@+id/error_simple_no_internet_connection"
        layout="@layout/no_internet_connection_layout"
        android:visibility="gone" />

    <include
        android:id="@+id/error_simple_simple_list"
        layout="@layout/simple_list_layout"
        android:visibility="gone" />

    <include
        android:id="@+id/error_simple_empty_layout"
        layout="@layout/empty_layout"
        android:visibility="gone" />
</FrameLayout>